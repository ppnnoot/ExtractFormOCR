# ✅ อัปเดตระบบ: ใช้ Form ID เท่านั้น

## 🎯 สิ่งที่เปลี่ยน

ระบบปรับปรุงให้ **ใช้ Form ID โดยตรง** เพื่อเพิ่มประสิทธิภาพ

## 📋 Form IDs

| Form ID | ประเภท |
|---------|--------|
| `HL0000050` | Receipt-Bill |
| `HL0000052` | Invoice |
| `HL0000053` | Detail |
| `HL0000054` | Estimate |
| `HL0000055` | Statement |

## 💻 การใช้งาน

```python
import requests

# ส่ง Form ID โดยตรง
response = requests.post(
    "http://localhost:8888/extract/text",
    json={
        "ocr_texts": ["ใบเสร็จรับเงิน", "..."],
        "form_id": "HL0000050"  # ใช้ Form ID
    }
)
```

## 📚 เอกสาร

1. **การใช้งาน** - `FORMID_ONLY_USAGE.md`
2. **Migration Guide** - `MIGRATION_GUIDE.md` (สำหรับอัปเดตโค้ดเดิม)

## ⚡ Quick Test

```bash
curl -X POST "http://localhost:8888/extract/text" \
  -H "Content-Type: application/json" \
  -d '{"ocr_texts": ["test"], "form_id": "HL0000050"}'
```

## 📈 ประโยชน์

- ⚡ เร็วขึ้น - ไม่มี mapping overhead
- 🎯 ชัดเจน - ใช้ ID เดียวกันทั้งระบบ
- 🔧 ง่ายขึ้น - API ตรงไปตรงมา

---
**Version:** 2.1.0 | **Status:** ✅ พร้อมใช้งาน

