# ⚖️ Configuration Comparison

## 📊 เปรียบเทียบการตั้งค่า 3 แบบ

---

## 1️⃣ **FAST MODE** (เน้นความเร็ว)

```json
{
  "timeout": 120,
  "max_retries": 2,
  "max_tokens": 1000,
  "max_ocr_results": 20,
  "filter_low_confidence": true,
  "confidence_threshold": 0.6
}
```

| Metric | Value |
|--------|-------|
| ⏱️ **เวลา** | 5-8 วินาที |
| 📊 **ความครบถ้วน** | 70% |
| 📄 **รองรับหน้า** | 1 หน้า |
| 📝 **Billing items** | 1-5 รายการ |
| 💰 **ต้นทุน** | ต่ำ |

**เหมาะกับ:**
- ✅ Real-time API
- ✅ เอกสารสั้น
- ✅ ต้องการความเร็ว

---

## 2️⃣ **BALANCED MODE** (สมดุล)

```json
{
  "timeout": 180,
  "max_retries": 2,
  "max_tokens": 1500,
  "max_ocr_results": 50,
  "filter_low_confidence": true,
  "confidence_threshold": 0.5
}
```

| Metric | Value |
|--------|-------|
| ⏱️ **เวลา** | 10-15 วินาที |
| 📊 **ความครบถ้วน** | 80% |
| 📄 **รองรับหน้า** | 1-2 หน้า |
| 📝 **Billing items** | 1-10 รายการ |
| 💰 **ต้นทุน** | ปานกลาง |

**เหมาะกับ:**
- ✅ ใช้งานทั่วไป
- ✅ เอกสารปานกลาง
- ✅ สมดุลระหว่างเร็วและครบ

---

## 3️⃣ **COMPLETE MODE** (เน้นความครบถ้วน) ⭐ **ใช้อยู่ตอนนี้**

```json
{
  "timeout": 300,
  "max_retries": 3,
  "max_tokens": 2500,
  "max_ocr_results": 100,
  "filter_low_confidence": false,
  "confidence_threshold": 0.3
}
```

| Metric | Value |
|--------|-------|
| ⏱️ **เวลา** | 17-30 วินาที |
| 📊 **ความครบถ้วน** | 90% |
| 📄 **รองรับหน้า** | 1-4 หน้า |
| 📝 **Billing items** | 1-20 รายการ |
| 💰 **ต้นทุน** | สูง |

**เหมาะกับ:**
- ✅ Batch processing
- ✅ เอกสารยาว/ซับซ้อน
- ✅ ต้องการความแม่นยำสูงสุด
- ✅ **ช้าก็ไม่เป็นไร เพื่อความครบถ้วน** ⭐

---

## 📈 เปรียบเทียบโดยตรง

| Feature | FAST | BALANCED | **COMPLETE** ⭐ |
|---------|------|----------|----------------|
| **Timeout** | 120s | 180s | **300s** |
| **Max Retries** | 2 | 2 | **3** |
| **Max Tokens** | 1000 | 1500 | **2500** |
| **Max OCR Results** | 20 | 50 | **100** |
| **Filter Low Conf** | Yes | Yes | **No** |
| **Conf Threshold** | 0.6 | 0.5 | **0.3** |
| | | | |
| **เวลาเฉลี่ย** | 5-8s | 10-15s | **17-30s** |
| **ความครบถ้วน** | 70% | 80% | **90%** |
| **เอกสารยาว** | ❌ | ⚠️ | **✅** |
| **Billing เยอะ** | ❌ | ⚠️ | **✅** |

---

## 🎯 การเลือกใช้

### **ใช้ FAST MODE เมื่อ:**
- ต้องการความเร็วสูงสุด
- เอกสารสั้น (1 หน้า)
- Real-time processing
- ข้อมูลพื้นฐานเท่านั้น

### **ใช้ BALANCED MODE เมื่อ:**
- ใช้งานทั่วไป
- เอกสารปานกลาง (1-2 หน้า)
- ต้องการสมดุล
- ไม่แน่ใจว่าจะใช้แบบไหน

### **ใช้ COMPLETE MODE เมื่อ:** ⭐
- **ต้องการข้อมูลครบถ้วนที่สุด**
- **ช้าก็ไม่เป็นไร**
- เอกสารยาว (2-4 หน้า)
- Billing items เยอะ (10-20 รายการ)
- Batch processing
- ข้อมูลซับซ้อน

---

## 🔄 วิธีเปลี่ยน Mode

### **เปลี่ยนเป็น FAST MODE:**
```json
{
  "timeout": 120,
  "max_retries": 2,
  "max_tokens": 1000,
  "max_ocr_results": 20,
  "filter_low_confidence": true,
  "confidence_threshold": 0.6
}
```

### **เปลี่ยนเป็น BALANCED MODE:**
```json
{
  "timeout": 180,
  "max_retries": 2,
  "max_tokens": 1500,
  "max_ocr_results": 50,
  "filter_low_confidence": true,
  "confidence_threshold": 0.5
}
```

### **เปลี่ยนเป็น COMPLETE MODE:** (ใช้อยู่ตอนนี้)
```json
{
  "timeout": 300,
  "max_retries": 3,
  "max_tokens": 2500,
  "max_ocr_results": 100,
  "filter_low_confidence": false,
  "confidence_threshold": 0.3
}
```

**หลังแก้ `config.json` ต้อง Restart API Server!**

---

## 💡 Tips

1. **เริ่มจาก BALANCED** แล้วค่อยปรับ
2. **Monitor เวลา** ด้วย logs
3. **Check ความครบถ้วน** ของข้อมูล
4. **ปรับตามความต้องการ** จริง

---

**ตอนนี้ใช้:** **COMPLETE MODE** ⭐  
**เป้าหมาย:** ความครบถ้วน 90%+  
**Trade-off:** ยอมช้าเพื่อครบถ้วน ✅

---

**อัปเดต:** 10 ตุลาคม 2025  
**Version:** 2.2.0

